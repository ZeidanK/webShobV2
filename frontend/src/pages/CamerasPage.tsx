export default function CamerasPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Cameras</h1>
      <p>Camera management and live video interface - Coming in future slices</p>
      <p><em>Operators can view live streams. Admins can configure cameras.</em></p>
    </div>
  );
}